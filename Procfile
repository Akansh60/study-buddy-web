web: gunicorn app:app
gunicorn = production web server
           (better than Flask's built-in server for deployment)
app:app  = filename:flask_app_variable
           first "app"  = your file is app.py
           second "app" = your Flask variable is app = Flask(__name__)